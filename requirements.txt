# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
# ⚠️ pydantic-settings는 사용하지 않음 (RecursionError 문제)
# config.py에서 os.getenv()로 직접 환경변수 로드

# Vector search and embeddings
faiss-cpu==1.7.4
# ⚠️ NumPy 2.x는 FAISS와 호환 안됨 - 반드시 1.x 사용
numpy==1.26.4
# ⚠️ sentence-transformers를 5.1.1로 업그레이드 (huggingface_hub 호환성 해결)
sentence-transformers==5.1.1

# OpenAI (optional)
openai==1.3.7

# HTTP requests
requests==2.31.0

# Date and time handling
python-dateutil==2.8.2

# Environment variables
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Development
python-multipart==0.0.6
