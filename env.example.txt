# ====================================
# AI-CPM Project Environment Variables
# ====================================

# ========================================
# OpenAI Configuration (Required for LLM)
# ========================================
OPENAI_API_KEY=your_openai_api_key_here

# ========================================
# Global LLM Settings (모든 에이전트 기본값)
# ========================================
LLM_MODEL=gpt-4o-mini
# 선택 가능한 모델:
# - gpt-4o-mini      (추천! 빠르고 저렴)
# - gpt-3.5-turbo    (저렴함)
# - gpt-4            (최고 품질, 매우 비쌈)
# - gpt-4-turbo      (GPT-4보다 빠름)
# - gpt-4o           (최신 모델)

LLM_TEMPERATURE=0.7
# 0.0 = 일관된 응답 (정확성 우선)
# 1.0 = 창의적인 응답 (다양성 우선)
# 추천: 0.5-0.8

LLM_MAX_TOKENS=2000
# 응답 최대 길이 (토큰 수)
# 추천: 1000-3000

# ========================================
# 에이전트별 LLM 설정 (선택사항)
# ========================================
# 각 에이전트마다 다른 모델/설정 사용 가능
# 설정하지 않으면 위의 Global 설정 사용

# Supervisor (의도 분석 - 빠른 모델 추천)
SUPERVISOR_MODEL=gpt-4o-mini
SUPERVISOR_TEMPERATURE=0.3
# 낮은 temperature로 일관된 라우팅

# Law RAG Agent (법규 해석 - 정확한 모델 추천)
LAW_RAG_MODEL=gpt-4o-mini
LAW_RAG_TEMPERATURE=0.5
# 중간 temperature로 정확하면서도 이해하기 쉽게

# CPM Weather Cost Agent (권장사항 생성 - 창의적)
CPM_MODEL=gpt-4o-mini
CPM_TEMPERATURE=0.8
# 높은 temperature로 다양한 대응 방안 제시

# Merger Agent (요약 생성 - 명확한 전달)
MERGER_MODEL=gpt-4o-mini
MERGER_TEMPERATURE=0.6
# 중간 temperature로 명확하고 읽기 쉽게

# ========================================
# 시나리오별 설정 예제
# ========================================

## 시나리오 1: 저비용 운영 (전체 gpt-3.5-turbo)
# LLM_MODEL=gpt-3.5-turbo
# SUPERVISOR_MODEL=gpt-3.5-turbo
# LAW_RAG_MODEL=gpt-3.5-turbo
# CPM_MODEL=gpt-3.5-turbo
# MERGER_MODEL=gpt-3.5-turbo

## 시나리오 2: 하이브리드 (중요한 곳만 GPT-4)
# LLM_MODEL=gpt-4o-mini
# SUPERVISOR_MODEL=gpt-4o-mini  # 빠른 라우팅
# LAW_RAG_MODEL=gpt-4           # 정확한 법규 해석
# CPM_MODEL=gpt-4               # 정확한 권장사항
# MERGER_MODEL=gpt-4o-mini      # 요약은 간단히

## 시나리오 3: 최고 품질 (비용 무관)
# LLM_MODEL=gpt-4o
# SUPERVISOR_MODEL=gpt-4o
# LAW_RAG_MODEL=gpt-4
# CPM_MODEL=gpt-4
# MERGER_MODEL=gpt-4o

## 시나리오 4: 개발/테스트 (기본 설정, 추천!)
# LLM_MODEL=gpt-4o-mini
# (모든 에이전트 gpt-4o-mini 사용)

# ========================================
# Embeddings (FAISS용 - 선택사항)
# ========================================
USE_OPENAI_EMBEDDINGS=false

# ========================================
# FAISS Configuration
# ========================================
FAISS_INDEX_PATH=./data/faiss/index.faiss
FAISS_META_PATH=./data/faiss/meta.jsonl
FAISS_TOP_K=5

# ========================================
# External API Endpoints (Placeholders)
# ========================================
KMA_ENDPOINT=<KMA_ENDPOINT>
KMA_API_KEY=
HOLIDAY_ENDPOINT=<HOLIDAY_ENDPOINT>
HOLIDAY_API_KEY=

# ========================================
# Development Settings
# ========================================
USE_STUB=true
# true = stub 데이터 사용 (외부 API 불필요)
# false = 실제 외부 API 사용

CURRENCY=KRW
